type Movie {
    id: Int
    title: String
    rating: Int
    year: Int
    summary: String
    medium_cover_image: String
    language: String
}

type Query{
    movies: [Movie]
    movie(id: Int!): Movie
}